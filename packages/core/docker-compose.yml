version: "3"
services:
    app:
        extends:
            file: ../../docker-compose.yml
            service: app
        image: iniquitybbs/core:latest
        volumes:
            - ./dist:/iniquity
            - ./data:/sbbs/data
